
  # Yellow Stroke Website Design

  This is a code bundle for Yellow Stroke Website Design. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  